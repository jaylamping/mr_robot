# Link

The primary control interface for the humanoid robot. Link is a React web app served directly from the robot, providing real-time motor telemetry, arm coordination, and direct actuator control.

## Stack

- **React 19** with TypeScript 5.9
- **Vite 8** — build tooling and dev server
- **TanStack Router** — file-based routing with auto code-splitting
- **Tailwind CSS 4** — styling (via Vite plugin, no separate config)
- **Zustand** — lightweight state management
- **Recharts** — time-series telemetry charts
- **shadcn/ui** (Base Nova style) — component primitives
- **Lucide** — icons
- **Sonner** — toast notifications
- **Node v24** (see `.nvmrc`)

## Getting Started

### Prerequisites

- Node.js v24+ (or use `nvm use` to pick up `.nvmrc`)
- The `link-server` backend running on port 8080

### Install

```sh
cd link
npm install
```

### Development

Start the Vite dev server with HMR on port 5173. API requests are proxied to the backend at `localhost:8080`.

```sh
npm run dev
```

To run the backend in mock mode (no hardware required):

```sh
cargo run -p link-server --bin link -- --no-hardware
```

Then open http://localhost:5173.

### Production Build

```sh
npm run build
```

This runs `tsc` (type-check only, no emit) then `vite build`. Output goes to `dist/`, which the `link-server` binary serves automatically at http://localhost:8080.

## Project Structure

```
src/
├── main.tsx                   App entry — router, providers, toaster
├── index.css                  Tailwind imports and CSS variables
├── routeTree.gen.ts           Generated by TanStack Router plugin (do not edit)
├── routes/
│   ├── __root.tsx             Shell layout — sidebar nav, WebTransport connection
│   ├── index.tsx              Overview — motor card grid, robot diagram
│   ├── motor.$id.tsx          Per-motor detail — telemetry chart, control panel
│   ├── arms.tsx               Arm coordination — enable/disable, pose editor
│   ├── system.tsx             Server status, config summary
│   ├── settings.tsx           Robot configuration viewer
│   └── logs.tsx               Live log viewer with auto-refresh
├── components/
│   ├── MotorCard.tsx           Motor status summary card
│   ├── MotorControl.tsx        Enable/disable, move, MIT control panel
│   ├── TelemetryChart.tsx      Real-time position/velocity/torque chart
│   ├── PoseEditor.tsx          Joint angle sliders for arm posing
│   ├── RobotDiagram.tsx        Visual robot body map
│   └── ui/                     shadcn/ui primitives (button, card, dialog, etc.)
├── hooks/
│   └── useWebTransport.ts      WebTransport connection to telemetry stream
├── stores/
│   └── telemetry.ts            Zustand store for real-time motor telemetry
└── lib/
    ├── api.ts                  REST API client (typed fetch wrappers)
    └── utils.ts                Tailwind merge helper
```

## API

Link communicates with `link-server` over two channels:

### REST (`/api/*`)

| Endpoint | Method | Description |
|---|---|---|
| `/api/config` | GET | Robot configuration |
| `/api/status` | GET | Server uptime, mode, motor count |
| `/api/motors` | GET | List all motors |
| `/api/motors/:id` | GET | Motor detail (angle, velocity, torque, temp) |
| `/api/motors/:id/enable` | POST | Enable motor |
| `/api/motors/:id/disable` | POST | Disable motor |
| `/api/motors/:id/zero` | POST | Zero motor encoder |
| `/api/motors/:id/move` | POST | Position move (`{ position_rad, kp?, kd? }`) |
| `/api/motors/:id/control` | POST | MIT control (`{ position, velocity, kp, kd, torque }`) |
| `/api/arms` | GET | List arms with joint info |
| `/api/arms/:side/enable` | POST | Enable all joints on arm |
| `/api/arms/:side/disable` | POST | Disable all joints on arm |
| `/api/arms/:side/home` | POST | Home arm to neutral position |
| `/api/arms/:side/pose` | POST | Set arm pose (`{ joints: {name: rad}, kp?, kd? }`) |
| `/api/logs` | GET | Recent log entries (`?limit=N`) |

### WebTransport (real-time telemetry)

Motor feedback (position, velocity, torque, temperature) is streamed as datagrams over a WebTransport/QUIC connection. The `useWebTransport` hook manages the connection and feeds data into the `telemetry` Zustand store.

## Path Aliases

`@/` maps to `src/` in both TypeScript and Vite:

```ts
import { Button } from "@/components/ui/button"
```
